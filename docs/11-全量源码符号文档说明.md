# 全量源码符号文档说明

你要求“全部”，本目录提供可检索的**全量符号索引**（自动生成）：

1. `docs/reference/gms-server-full-symbols.md`
   - 覆盖 `gms-server` 的 Java 文件（`org/gms`）
   - 包含类型声明、字段候选、方法/构造器候选（含非 public）
2. `docs/reference/gms-ui-full-symbols.md`
   - 覆盖 `gms-ui/src` 的 TS 与 Vue script block
   - 包含 export、function、箭头函数候选
3. `docs/reference/beidou-ijl15-full-symbols.md`
   - 覆盖 `ezorsia` 下 C/C++ 文件
   - 包含 class、类方法定义、顶层函数候选

## 当前规模（本次生成）

- gms-server 全量索引：约 2.8 万行
- gms-ui 全量索引：约 1.3 千行
- ijl15 全量索引：约 1.4 千行

> 注意：全量文档采用正则与结构层级结合的启发式提取，目标是“尽可能全 + 可搜索”。  
> 某些多行声明、宏展开、生成代码（如 Lombok）可能存在漏检，语义解释仍应结合源文件与 IDE。

## 生成方式

```bash
export BEIDOU_SERVER_REPO=/path/to/BeiDou-Server
export BEIDOU_IJL15_REPO=/path/to/BeiDou-ijl15
python3 scripts/generate-full-symbol-docs.py
```

## 与已有文档的关系

- 架构与阅读路径：`docs/09-源码深度导读.md`
- ijl15 叙述性细节：`docs/10-BeiDou-ijl15-源码详解.md`
- gms-server public 索引（较精简）：`docs/reference/gms-server-api-index.md`

## 逐符号中文职责版（自动生成，非“读代码写出的理解”）

- **gms-server（已按模块分卷）**
  - 分卷索引：`docs/reference/gms-server-symbol-notes-README.md`
  - 各卷目录：`docs/reference/gms-server-symbol-notes/`
  - 根文件 `docs/reference/gms-server-symbol-notes.md` 仅作跳转说明
- `docs/reference/gms-ui-symbol-notes.md`
- `docs/reference/beidou-ijl15-symbol-notes.md`

上述 `*-symbol-notes.md` 由脚本按**命名规则**批量生成一句话摘要，**不能**替代阅读实现；空文件或计数为 0 的段落无解释价值，请忽略。

**真正按理解写的模块说明**见：`docs/12-源码模块精读-人机撰写.md`（可随指定模块继续扩写）。

